import React from "react";
import { Typography } from "antd";

const { Title } = Typography;

const QualityPage: React.FC = () => {
  return (
    <div>
      <Title level={2}>Quality</Title>
    </div>
  );
};

export { QualityPage };

