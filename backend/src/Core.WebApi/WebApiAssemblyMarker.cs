namespace MyIS.Core.WebApi
{
    /// <summary>
    /// Маркерный класс для интеграционных тестов (WebApplicationFactory<WebApiAssemblyMarker>).
    /// Не содержит логики, используется только как ссылка на сборку WebApi.
    /// </summary>
    public sealed class WebApiAssemblyMarker
    {
    }
}